{
  "name": "relayfile-providers",
  "version": "0.1.0",
  "private": true,
  "description": "Relayfile providers — auth and credential management for external services",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx turbo build",
    "test": "npx turbo test",
    "typecheck": "npx turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "npm@10.9.4"
}
