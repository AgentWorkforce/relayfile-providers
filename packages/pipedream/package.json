{
  "name": "@relayfile/provider-pipedream",
  "version": "1.0.0",
  "description": "Pipedream provider for Relayfile with generic provider hooks and Pipedream Connect convenience methods.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "npm run build && node --test 'dist/__tests__/**/*.test.js'",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [
    "relayfile",
    "pipedream",
    "provider",
    "connect"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@relayfile/sdk": "file:../../AgentWorkforce-relayfile/packages/sdk/typescript"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  }
}
